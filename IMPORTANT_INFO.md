# IMPORTANT INFO

- `/topics/[topic]` now builds a cached topic index once and revalidates it weekly (`604800` seconds).
- README render cost was reduced by truncating README preview content before markdown render.
- README render cost was reduced by disabling raw HTML markdown rendering (`skipHtml`).
- README render cost was reduced by skipping README preview rendering for likely crawler user agents.
